#ifndef INIT
#define INIT

/*
** CLI entry point for `mygit init`.
**
** Parameters:
** - argc: original process argument count
**
** Returns:
** - 0 on success
** - -1 on invalid usage or filesystem failure
*/
int init(int argc);
#endif
